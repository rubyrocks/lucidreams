<%= form_for(@dream) do |f| %>
  <% if @dream.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dream.errors.count, "error") %> prohibited this dream from being saved:</h2>

      <ul>
      <% @dream.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.select :category, Dream::DREAM_TYPES %>

  </div>
  <div class="field">
    <%= f.label :dream %><br />
    <%= f.text_area :dream %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
